# LUMINA LIVE — 現在の状態と本番までの準備

2026年9月9日更新。Windows版と単一HTML版を生成し、両方の同期モードを搭載しました。詳しい起動は[起動ガイド](docs/launch.md)、操作は[ライブガイド](docs/live-guide.md)、出典と取得状況は[素材ガイド](docs/material-sources.md)を参照してください。

## すぐ試す

この作業PCで[操作画面](http://127.0.0.1:4173/)を開き、「▶ 再生」でデモを動かします。素材をA/Bへ読み込んで「AUTO」で切り替えます。操作画面は1つだけ開き、投影画面は「外部出力」から開いてください。このアドレスは作業PC上のローカルサーバーが動いている間だけ使えます。

Windowsでは `Lumina-Live.exe` と隣の `assets` を一緒に持ち運びます。Chromebookでは `web/Lumina-Live.html` をChromeで開き、必要な動画を取り込みます。[GitHub](https://github.com/sakadosoutan-boop/lumina-live)は非公開で、ソースとHTMLを保存しています。

## 素材

**2026年9月9日更新。** 外部動画251本を取得し、243本をアプリのカタログに登録しました。内蔵96種類と合わせ、選択できる映像は339種類です。

| 区分 | 本数 | 内訳 |
| --- | ---: | --- |
| 内蔵映像 | 96種類 | 12系統×8変種のオリジナルGPU映像。動画ファイルの取得数には含みません。 |
| open素材 | 118本 | Mantissa 92本、NASA SVS 20本、NOAA 2本、USGS・NPS・ESO・ESA/Hubble 各1本。 |
| Beepleのパック | 93本 | MANIFEST 66本、four.color.process 10本、Brainfader 8本、Resolume 9本。 |
| Neb Motionのパック | 40本 | Abstract Tunnels 1・2、Retro Sunsets 1、Abstract Geometry 1を各10本。 |
| アプリで選べる外部動画 | 243本 | 取得原本251本から、個別レビュー前の8本を除いた数。 |
| 登録から除外した動画 | 8本 | MANIFESTの名前にORGYを含む素材。原本は保持。内容を名前だけで断定したものではありません。 |

取得原本と軽量な再生用ファイルは同じ映像なので、取得本数へ重複して加算しません。配布ページの予定数と実際のZIP内の本数が異なる場合も、実ファイルで数えています。CanvaとVideoZeroのプロジェクトはローカルMP4未取得のため、この本数には含めません。

[最終集計](research/library-audit.json)で、取得した原本のハッシュ、投影用動画の全編デコード、指定20 GB以内の素材容量を確認できます。元の動画と変換版の両方を保持しています。ブラウザーや別端末へ取り込んだ追加コピーの容量は別に管理してください。

CanvaはFreeの10秒・1080pトンネル映像のデザインと画面上の書き出し、VideoZeroは無音8.1秒のオリジナル幾何学プロジェクトを作成済みです。どちらもローカルMP4は未取得です。[Canva](https://www.canva.com/design/DAHUkLu0GjM/FE9xFwz7zZ9PYiCNG62QvA/edit)、[VideoZero](https://videozero.ai/view/?id=1c4c6876-0c65-45e8-a832-de1bdf17b65f)

## 検証

最新版は**自動テスト366件合格、型検査・本番ビルド・Windows版と単一HTMLの生成成功、npm auditの検出0件**です。ブラウザーではLIVE・AUDIO、実際のMP4再生、AUTOクロスフェード、別画面への出力、歌詞表示、BLACKOUTを操作して確認しました。

全243本の再生用動画に対し、FFmpegで全編をデコードする検査を実施しました。これはファイルの技術的な読み込み検査です。全編の内容レビュー、ループの見え方、本番端末での描画性能は別に確認してください。

Surface Pro 7+／Chromebookの実機、物理HDMI、Windows EXEの直接起動は未確認です。単一HTMLの `file://` 直接起動のUI検証はブラウザー操作ツールのURLポリシーでブロックされました。同じアプリのHTTP配信で確認した動作を、これらの実機試験の代わりにはしていません。

## 本番までに行うこと

1. 使用するSurface Pro 7+またはChromebookで、起動・保存・再開・オフライン利用を確認します。音源は再リンクします。
2. HDMIを拡張表示、プロジェクターを1920×1080にし、操作画面と投影画面、音声の出力先を確認します。内部描画は1280×720／30 FPSから始めます。
3. 使用素材の内容、ループのつなぎ目、明るさ、必要なクレジットを確認します。自動VJの候補も曲ごとにリハーサルします。
4. 全曲を通し、LIVEのテンポ・歌詞補正と、AUDIOの音源時刻に合わせた歌詞を調整します。MIDI／OSCを使う場合は当日の機器で試します。
5. 長時間運転、暗転、フリーズ、出力ウィンドウの再接続を確認してから本番へ持ち込みます。

## 同期と機能の範囲

LIVEは生演奏を基準に操作する人が補正します。AUDIOは伴奏音源の時刻を基準にし、歌詞ジャンプ時は音源も移動します。歌詞・曲長・BPMからの自動配置は文字量と小節に基づく推定で、音声認識による自動追唱ではありません。LRC／SRTの読み込み、キュー時刻の記録と編集で合わせます。

録画は合成映像の無音WebMで、256 MiBを超えた後に停止します。自動連続分割、各レイヤー別録画、カメラ入力、MTC／LTC／NDI／Spoutは未対応です。その他の搭載機能は[README](README.md)に記載しています。
